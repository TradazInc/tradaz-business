import { StoreForm } from "@/app/ui/store/StoreForm";
import { Center } from "@chakra-ui/react";

const page = () => {
  return (
    <Center h={"full"} w={"full"}>
      <StoreForm />
    </Center>
  );
};

export default page;
